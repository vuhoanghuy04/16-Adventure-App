<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DuolingoApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.OnboardingActivity" android:exported="false" />

        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.LanguageSelectionActivity" />
        <activity android:name=".activities.SetupGoalActivity" />
        <activity android:name=".activities.StudyPlanActivity" />
        <activity android:name=".activities.GameMenuActivity" />
        <activity android:name=".activities.GhepTuActivity" />
        <activity
            android:name=".activities.ScrambleGameActivity" />
        <activity
            android:name=".activities.QuizGameActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.MemoryGameActivity" />

        <activity
            android:name=".activities.ComprehensiveExamActivity"
            android:exported="false" />
        <activity android:name=".activities.ExamSelectionActivity" />

        <!-- Added Lesson Selection Activity -->
        <activity android:name=".activities.LessonSelectionActivity" />

        <activity
            android:name=".activities.ProfileActivity" />

        <activity
            android:name=".activities.EditProfileActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activities.FlashcardActivity"
            android:label="Flashcards" />
        <activity
            android:name=".activities.AddVocabularyActivity"
            android:label="Add Word" />

        <activity android:name=".activities.GrammarListActivity" />
        <activity android:name=".activities.GrammarActivity" />
        <activity android:name=".activities.PracticeActivity" />
        <activity android:name=".activities.ListeningListActivity" />
        <activity android:name=".activities.ListeningActivity" />
        <activity android:name=".activities.PronunciationListActivity" />
        <activity android:name=".activities.PronunciationActivity" />
        <activity android:name=".activities.ChatAssistantActivity" android:windowSoftInputMode="adjustResize" />

        <receiver android:name=".services.NotificationReceiver" />

    </application>

</manifest>
