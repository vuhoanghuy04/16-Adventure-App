<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._16Adventure">

        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.DetailActivity" />
        <activity android:name=".activities.ArticleDetailActivity" android:exported="false" />
        <activity android:name=".activities.MapActivity" />

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />

        <activity android:name=".activities.LoginActivity" />

        <activity android:name=".activities.ExploreActivity" />
        <activity android:name=".activities.KnowledgeActivity" />

        <activity android:name=".activities.AiActivity" />
        <activity android:name=".activities.QuizActivity" />
        <activity android:name=".activities.SavedMonumentsActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.RecognitionActivity" />
        <activity android:name=".activities.RecognitionResultActivity" />
        <activity android:name=".activities.OfflineMapActivity" />
        <activity android:name=".activities.JournalListActivity" />
        <activity android:name=".activities.AddJournalActivity" />
        <activity android:name=".activities.EventDetailActivity" />
        <activity android:name=".activities.EventListActivity" />
        <activity android:name=".activities.LandmarkGalleryActivity" />
        <activity android:name=".activities.MediaLibraryActivity" />
        <activity android:name=".activities.PhotoViewerActivity" />
        <activity android:name=".activities.VideoPlayerActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>
